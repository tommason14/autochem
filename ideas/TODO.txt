# TODO

- When creating inputs, call with a file and call instance of molecule:
	- ...input(using=file)
			mol = Molecule(using)
			mol.separate() --> coords
			mol.gamess_format --> give mol.indat, mol.icharg
			frag_inp = GamessInput(type="optimisation") or maybe
			frag_inp = PsiInput(type="single point")
			then:
			mol.make_frag_inputs(frag_inp)
			
			
- make a way for meta.json to be used to look up run types, and fmo etc...


- for command line arguments:

Place all scripts in a scripts directory

create a menu:
1. Create inputs recursively
2. Obtain results recursively
3. Get frequencies recursively
...

call for 1: run scripts/inputs.py
call for 2: run scripts/results.py